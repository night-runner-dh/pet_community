<div class="container px-5 px-sm-0">
   <div class="row">  
 <div class="col-md-8 offset-md-1">
  
<h2>ユーザー詳細</h2>
<div class = "mt-4">プロフィール画像
<%= image_tag "shiba1.jpg", size: "200x200" %>
</div>

<table class="table mt-4">
  <tr><%#= image_tag user.get_profile_image, size:'100x100' %></tr>
  <tr>
	  <th>ハンドルネーム:</th>
	  <th><%= @user.name %></th>
  </tr>
  <tr>
    <th>フォロー</th>
    <th><%#= link_to user.followings.count, user_followings_path(user) %></th>
            <td>フォロー数: <%= @user.followings.count %></td>
        <td>フォロワー数: <%= @user.followers.count %></td>
        <td><%= render "public/relationships/btn", user: @user, options: { class: "follow-link" } %></td>
    
    
    
  </tr>
  <tr>
    <th>フォロワー</th>
    <th>ここにフォロワーのリンクが来るよ
      <%#= link_to user.followers.count, user_followers_path(user) %></th>
  </tr>
   <td><%#= render "relationships/btn", user: user, options: { class: "follow-link" } %></td>
   <td></td>
</table>
