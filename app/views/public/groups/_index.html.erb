<table class="table">
  <thead>
    <tr>
      <th></th>
      <th>グループ名</th>
      <th>メンバー数</>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% groups.each do |group| %>
    <tr>
      <td></td>
      <td><%= link_to group.name, group_path(group) %></td>
      <td><%= group.users.count %></td>
      <td></td>
    </tr>
    <% end %>
  </tbody>
</table>